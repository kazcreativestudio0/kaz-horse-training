# パフォーマンス問題の原因一覧

## 🔴 重大な問題（最優先で修正）

### 1. **backdrop-filter: blur() の多用**
- **場所**: `index.html` の `.anti-gravity-card` と `.glass-logo`
- **問題**: `blur(20px)` は非常に重い処理で、GPU負荷が高い
- **影響**: ページ全体の描画が遅くなる
- **修正**: blur値を減らすか、削除する

### 2. **大量の画像の即座読み込み**
- **場所**: `constants.ts` の `import.meta.glob` で `eager: true`
- **問題**: 全ての画像をビルド時に一度に読み込む
- **影響**: 初回読み込みが非常に遅い
- **画像数**: 
  - horsesフォルダ: 13枚
  - galleryフォルダ: 3枚
  - その他多数
- **修正**: lazy loadingを実装

### 3. **重いグラデーションアニメーション**
- **場所**: `index.html` の `.anti-gravity-bg`
- **問題**: `gradientShift` アニメーションが15秒間無限ループ
- **影響**: CPU/GPU負荷が高い
- **修正**: アニメーションを削除または簡素化

## 🟡 中程度の問題

### 4. **多数のオーバーレイ要素**
- **場所**: `Hero.tsx` の背景に5つのオーバーレイdiv
- **問題**: レイヤーが多すぎて描画コストが高い
- **影響**: スクロール時のパフォーマンス低下
- **修正**: オーバーレイを統合

### 5. **3D transform効果**
- **場所**: `transform-3d`, `perspective`, `card-3d`
- **問題**: 3D変換は計算コストが高い
- **影響**: ホバー時のラグ
- **修正**: 2D transformに変更または削除

### 6. **未使用のアニメーション定義**
- **場所**: `index.html` の `@keyframes float`, `floatReverse`, `floatSlow`
- **問題**: 使用されていないが定義が残っている
- **影響**: メモリとCSS解析の無駄
- **修正**: 削除

### 7. **外部CDNリソース**
- **場所**: `index.html`
  - Tailwind CSS CDN
  - Google Fonts (4種類)
  - React/React-DOM CDN
  - Instagram embed script
- **問題**: 外部リソースの読み込み待ち
- **影響**: 初回読み込みが遅い
- **修正**: ローカルにバンドル

### 8. **複数のグラデーション背景**
- **場所**: Heroセクション、各セクション
- **問題**: 複雑なグラデーション計算
- **影響**: 描画コストが高い
- **修正**: シンプルな背景に変更

## 🟢 軽微な問題

### 9. **過剰なbox-shadow**
- **場所**: `.anti-gravity-card`, `.glass-logo`
- **問題**: 複数のbox-shadowが重なっている
- **影響**: 描画コストが高い
- **修正**: shadowを簡素化

### 10. **mix-blend-modeの使用**
- **場所**: Heroセクションのオーバーレイ
- **問題**: ブレンドモードは計算コストが高い
- **影響**: 描画が遅くなる
- **修正**: 削除または簡素化

### 11. **transition-allの多用**
- **場所**: 多数の要素
- **問題**: `transition-all` は全てのプロパティを監視
- **影響**: 不要な再描画
- **修正**: 必要なプロパティのみ指定

### 12. **画像の最適化不足**
- **問題**: JPG/PNG形式で、WebP/AVIF未使用
- **影響**: ファイルサイズが大きい
- **修正**: 画像をWebP/AVIFに変換

## 📊 推奨される修正順序

1. **backdrop-filterを削除または減らす** (最大の影響)
2. **画像のlazy loadingを実装**
3. **グラデーションアニメーションを削除**
4. **オーバーレイを統合**
5. **未使用のCSSを削除**
6. **3D transformを削除**
7. **外部CDNをローカルに移行**

